import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    secondary: {
      light: '#9ba4b4',
      main: '#394867',
      dark: '#14274e',
      contrastText: '#f1f6f9',
    },
  },
});

export default theme;
